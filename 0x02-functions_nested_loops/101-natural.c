# include "main.h"
/**
 * main - Entry
 * function to compute all multiples
 * Return: Always 0
 */
int main(void)
{
int x;
int sum = 0;
for (x = 0; x < 1024; x++;
sum = sum + x;
}
printf("%d\n", sum);
return (0);
